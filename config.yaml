server:
  host: "0.0.0.0"
  port: 8000

aria2:
  rpc_host: "localhost"
  rpc_port: 6800
  rpc_secret: ""

paths:
  media_dir: "/media"
  download_dir: "/downloads"

scrapers:
  tmdb:
    api_key: ""
  tvdb:
    api_key: ""
  douban:
    enabled: false
